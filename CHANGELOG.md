# 更新日志

## v2.9（2026-08-14）

### 网易中国版支持（重大）

- 上游 DLL 升级至 **MCCombatStatusJni V65**：**不再创建采集线程、不再调用 `AttachCurrentThread`**（外来原生线程附加 JVM 会触发 ThreadStart 事件，正是网易版反作弊强杀旧版的导火索）
- 新架构改为**钩住 `gdi32!SwapBuffers`**（LWJGL2/GLFW WGL 渲染路径的汇合点），在游戏自己的 Client thread 内用 `GetEnv()` 复用其已有 JNIEnv，解析/采样/上报全部帧驱动完成
- **网易中国版 1.20.1 Forge 真机验证通过**：注入后游戏存活、准星目标识别正确（SnowGolem → canAttack=1）、60+ 秒观察无强杀；网易窗口类同为 GLFW30，本程序可自动注入
- 共享内存与 UDP 协议零变化，状态刷新频率变为渲染帧率驱动（60fps≈16.6ms），300ms 陈旧阈值兼容

### 功能

- **CPS 预设新增 30/s、40/s 档位**（左右键独立，各 6 档：6/10/15/20/30/40）
- **光标检测门控**（标题栏光标图标开关）：开启后仅当系统光标不可见（游戏视角）时连点，打开背包/聊天/菜单等 GUI（光标可见）时自动暂停左右键连点，避免误点
- 移除「网易中国版无法使用」的界面提示与文档说明（V65 起已实测可用）

### 反检测加固（上游 DLL V66）

- 上游 DLL 升级至 **MCCombatStatusJni V66**：敏感字符串 XOR 混淆（防编译器常量折叠）、**导出表剥离**、PEB 摘链 + 全/基名抹除、PE 头内存擦除；共享内存与 UDP 协议不变（本程序只读共享内存，不调用导出函数，零改动）
- 新增独立 **injector.exe**（手动映射注入器）：不调用 LoadLibrary（无 LoadImage 回调）、PEB 模块链表无条目、DLL XOR 加密内嵌**不落盘**、OpenProcess 最小权限、共享内存反重复注入
- 用法：`injector.exe`（自动找 Minecraft 窗口）/ `injector.exe -pid <PID>` / `-title <子串>`；注意按 PID 时要指真正的 JVM 子进程（javapath 垫片会派生子进程）
- 说明：本程序**内置注入仍走 LoadLibrary 远程线程路径**（网易 1.20.1/1.21.8 真机已验证可用，且 DLL 已带 PEB 摘链等加固、模块枚举不可见）；手动映射由 injector.exe 提供，可按需单独使用

### 反检测加固（V67，2026-08-14 晚，针对网易反作弊实测机制）

- 网易反作弊机制（吾爱破解 ProcMon 实测）：`api-ms-win-crt-utility-l1-1-1.dll` 在**游戏启动时扫描桌面所有文件 + 后台运行进程的可执行文件**（用户态、无 Ring-0），扫描结果上报服务器 → 黑屋 → 封号
- 本程序注入路径**重写为手动映射**：不调用 LoadLibrary、PEB 模块链表无条目；DLL 构建期 XOR 加密内嵌 exe（`tools/enc_dll.ps1`），运行时内存解密注入——**不再解出 %TEMP% 下的 DLL，磁盘零痕迹**
- exe 更名 **InputTuner.exe**（进程文件名中性化，避免"AutoClicker/连点"类关键词被进程扫描命中）
- 反重复注入改用共享内存健康检查（手动映射无模块条目）；上游 DLL V66.1 默认关闭 PEB 摘链/头抹除（真机实锤被判"隐藏模块"进黑屋）、V66.2 ws2_32 动态解析（目标未加载时跳过 UDP 不再崩溃）
- **使用卫生要求**：工具与 DLL 不要放在桌面、桌面不留快捷方式；先启动游戏再启动工具；**账号一旦进过黑屋会持续一段时间，需换干净账号验证**

### 文档

- README（中/英）与 DEVELOPMENT.md 同步 V65 架构与网易版真机验证结果
- 新增本更新日志

## v2.8（2026-08-13）

### 全版本支持：1.8.8 ~ 1.21.11

- 升级 MCCombatStatusJni.dll（V64）：内置 **171 张映射表**（54 个 Minecraft 版本 × 4 命名空间），按 classpath 版本号自动定位版本
- 「仅能攻击时连点」与「仅手持放置物时右键连点」支持 **1.8.8 ~ 1.21.11 几乎全版本（原版 / Forge / Fabric / NeoForge）**
- 共享内存轮询主通道（`Local\MCCombatStatus_<PID>`，5ms 刷新）；UDP 35785 仅作旧版 DLL 兜底
- 面板页新增游戏状态实时显示：连接状态 / 游戏内 / 准星目标实体 / 命中类型
- 真机验证：原版 1.14、Forge 1.16.5、Fabric 1.16.5 / 1.21.11、NeoForge 1.20.4 / 1.21 / 1.21.11

## v2.5（2026-08-11）

- 仅手持放置物时右键连点（canPlace 门控，与仅能攻击时连点共用状态通道）
- DLL 更名 MCCombatStatusJni（共享内存 magic 'MCST' v7 + UDP 2 字节协议）
- HWID 使用调查上报（启动时 WinHTTP GET，失败静默）
- 启动时版本检查（有新版弹窗提示版本号 + 更新内容）
- 版本比较与显示规范化（v 前缀/预发布后缀处理，含单元测试）
- Net / Base 双版本构建（`AUTOCLICKER_NET` 宏区分）

## v2.4（2026-08-10）

- 修复点击页右键卡片 CPS 文本矩形反转（§7.10）
- 修复快捷键空值（0）被配置加载过滤导致"复活"（§7.11）
- 修复版本提示弹窗"双 v"显示（§7.12）

## v2.3（2026-08-08）

- 配置方案系统（4 槽 + 标题栏芯片切换 + 面板页重命名 + 切换热键）
- 拟人化节奏（均匀/双击连招/呼吸波动/疲劳递减 + 强度 1-5）
- 随机 CPS、CPS 上限、定时自动停止

## v2.0（2026-08-01）

- 玻璃态（Glassmorphism）UI：分层透明渲染、双主题、4 色强调色、Toast 动画
- 高性能连点引擎：1ms 定时器 + 亚毫秒精确休眠（点击间隔 std≈0.073ms）
- 多倍点击、滚轮转点击、快捷键绑定、保持模式、窗口置顶/位置记忆
- 兼容输入法钩子（ANSI 窗口 API + 动态解析 PostMessageA）
